export type Professor = {
  matricula: string;
  docente: string;
  instituicao: string;
  unidade: string;
  departamento: string;
  titulacao: string;
  carga_horas: string;
  carga_minutos: string;
  vinculo?: string;
};
